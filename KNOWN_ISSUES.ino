/*
 *   ***FIXED*** Race type dissapears when selecting another item is blanked  fixed by calling topBar in blinkstate  
 *   ***FIXED*** Counter starts one second late after the warning counter hits 0
 *   Need to add beeps
 *   Need to add way to set time
 *   Need to add actual relay horns
 *  ***FIXED**** rolling time needs its own temp variable for correct reset off all 3 counters
 *   need to test whether counter stops if there is not roll counter
 *  with one minute on timer it sets to 60 instead of 1:00
 * 
 * 
 */
